{
  "name": "obs-new-day",
  "version": "1.0.0",
  "description": "Insert a dated h5 heading for stacked daily notes in one file.",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "0.25.5",
    "obsidian": "1.12.3",
    "tslib": "2.4.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
